#!/usr/bin/env bash
# create a tmp file and delte it on SIGQUIT and SIGTERM

echo "$$" > "/tmp/myscript.pid"
trap 'echo "I hate the kill ommand"; rm -f "/tmp/myscript.pid"; exit'  TERM
trap 'echo "Y U no love me?!"' INT
trap 'rm -f "/tmp/myscript.pid"; exit' QUIT
while true
do
        echo "To infinity and beyond"
        sleep 2
done
